
query getAll {
  getAllProducts {
    id
    title
    description
    price
    rentPrice
    rentType
    categories
    createdAt
  }
}

query getProductById($productId: Int!){
  getProduct(productId: $productId) {
    id
    title
    description
    price
    rentPrice
    rentType
    categories
    createdAt
  }
}