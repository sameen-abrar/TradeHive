
query getAll {
  getAllProducts {
    title
    description
    price
    rentPrice
    rentType
    categories
    createdAt
  }
}

query getProductById($productId: Int!){
  getProduct(productId: $productId) {
    title
    description
    price
    rentPrice
    rentType
    categories
    createdAt
  }
}